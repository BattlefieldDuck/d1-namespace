{
    "extends": "../tsconfig.json",
    "compilerOptions": {
        "moduleResolution": "bundler",
        "types": [
            "@cloudflare/vitest-pool-workers" // For `cloudflare:test` types
        ]
    },
    "include": [
        "./**/*.ts",
        "../worker-configuration.d.ts", ".spec copy.ts", // output of `wrangler types`
    ]
}